package io.shaderfoundry.demo

import android.Manifest

const val PROVIDER_AUTHORITY = "io.shaderfoundry.fileprovider"
val CAMERAX_PERMISSIONS = arrayOf(
    Manifest.permission.CAMERA,
    Manifest.permission.RECORD_AUDIO,
)

